import Ember from 'ember';

export default Ember.Controller.extend({
  // filter: '',
  // filterPlayers: '',
  // filteredResults: function() {
  //   var filter = this.get('filter');
  //   return this.get('players').filter(function(team) {
  //     return team.indexOf(filter) !== -1;
  //   });
  // }.property('filter'),
  // actions: {
  //   applyFilter: function() {
  //     this.set('filter', this.get('filterPlayers'));
  //   }
  // }
});
