{{!-- <div class="container">
  <ul class="nav nav-pills">
    {{#link-to 'schedule.scheduleSwarm.new' tagName="li"}}<a href>Add New</a>{{/link-to}}
  </ul>
</div> --}}
<div class="container-fluid">
  {{#each model as |scheduleSwarm|}}
    <div class="col-md-6">
      <div class="panel panel-default">
        <div class="panel-heading">
          <div class="panel-title">
            <h4 class="text-center">{{scheduleSwarm.teamGame}} - {{scheduleSwarm.home}} v {{scheduleSwarm.away}}</h4>
          </div>
        </div>
        <div class="panel-body">
          <span class="pull-left">
            <img class="home-logo" src="{{scheduleSwarm.homeLogo}}" alt="{{scheduleSwarm.home}}">
          </span>
          <span>
            Details of the upcoming game
          </span>
          <span class="pull-right">
            <img class="away-logo" src="{{scheduleSwarm.awayLogo}}" alt="{{scheduleSwarm.away}}">
          </span>
        </div>
        <div class="panel-footer">
          <h6><span class="pull-left">
            {{scheduleSwarm.venue}}
          </span>
          <span>
            Tickets
          </span>
          <span class="pull-right">
            {{scheduleSwarm.date}}
          </span></h6>
        </div>
      </div>
    </div>
  {{/each}}
</div>
