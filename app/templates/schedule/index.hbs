{{!-- <div class="container-fluid">
  {{#each filteredList as |schedule|}}
    <div class="col-md-6">
      {{#schedule-item item=schedule}}
        Venue: {{schedule.venue}} Date: {{schedule.date}}
      {{/schedule-item}}
    </div>
  {{/each}}
</div> --}}

<h2>List</h2>

<div class="row">
  <div class="col-md-1">
    {{#link-to (query-params limit='all' team='') class='btn btn-default btn-lg btn-block'}}All{{/link-to}}
  </div>
  <div class="col-md-1">
    {{#link-to (query-params limit='abc') class='btn btn-default btn-lg btn-block'}}Abc{{/link-to}}
  </div>

  <div class="col-md-10">
    {{#if limitAll}}
      <div class="form-group">
        {{input value=filter class="form-control input-lg" placeholder="Filter by schedule name"}}
      </div>
    {{!-- {{else}} --}}
        {{!-- <div class="form-group">
          {{abc-buttons}}
        </div> --}}
    {{/if}}
  </div>
</div>

<div class="row">
  {{#each filteredList as |schedule-item|}}
    <div class="col-md-4">
      {{#schedule-item item=schedule badge=schedule.home.length}}
        {{!-- {{#link-to 'libraries.edit' library.id class='btn btn-success btn-xs'}}Edit{{/link-to}} --}}
        {{!-- <button class="btn btn-danger btn-xs" {{action 'deleteLibrary' library}}>Delete</button> --}}
      {{/schedule-item}}
    </div>
  {{!-- {{else}}
    {{#if model.isUpdating}}
      {{spinner-cube}}
    {{else}}
      <div class="col-md-12 text-center">
        <div class="alert alert-info">
          Sorry, no result.
        </div>
      </div>
    {{/if}} --}}
  {{/each}}
</div>
